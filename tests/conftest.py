"""Окружение для тестов: роли не должны зависеть от личного .env разработчика.

`core/roles.py` читает ADMIN_USER_IDS / DIRECTOR_USER_IDS / ENGINEER_USER_IDS /
MANAGER_USER_IDS
из окружения один раз при импорте, а `bot.py` вызывает `load_dotenv()` до
этого. Пока значения приходили только из `.env`, набор проходил исключительно
на машине автора: у любого, кто клонировал репозиторий, и в CI файла нет,
`ADMIN_IDS` оказывался пустым, и тринадцать тестов падали со `StopIteration`
на `next(iter(ADMIN_IDS))`.

pytest импортирует conftest.py раньше тестовых модулей, поэтому значения,
выставленные здесь, попадают в `core.roles` при его импорте.
`load_dotenv()` уже существующие переменные не перезаписывает — реальный `.env`
эти подстановки не вытеснит, и тесты никогда не увидят боевых Telegram ID.

Идентификаторы намеренно не пересекаются с 111/222/333 из tests/test_roles.py,
который проверяет сам разбор ролей и мутирует окружение по ходу теста.
"""
import os

os.environ["ADMIN_USER_IDS"] = "900001"
# У руководителя подпись своя и задаётся рядом с ID: 900005 — с подписью,
# 900006 — без неё, чтобы проверялись оба пути (см. tests/test_bot_commands.py).
os.environ["DIRECTOR_USER_IDS"] = "900005:Руководитель CNC Electric,900006"
os.environ["ENGINEER_USER_IDS"] = "900002"
os.environ["MANAGER_USER_IDS"] = "900003,900004"
