# Поиск по СМЫСЛУ (semantic_reference.py) — необязательный слой.
#
# Ставится ОТДЕЛЬНО от основного requirements.txt, потому что тянет ONNX-
# рантайм и при первом запуске скачивает модель эмбеддингов (~220 МБ). Без
# этих зависимостей бот работает как раньше — поиском по словам; установка
# лишь добавляет второй проход «по смыслу» для переформулированных вопросов.
#
# Установка:
#     pip install -r requirements-semantic.txt
#
# При первом запросе бот один раз скачает модель
# sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 и закэширует её.
# Дальше всё работает офлайн. Проверить, включился ли поиск по смыслу, — /status.
fastembed>=0.3,<1.0
